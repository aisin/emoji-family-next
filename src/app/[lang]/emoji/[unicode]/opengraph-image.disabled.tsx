/**
 * Open Graph image route disabled for static export.
 *
 * To re-enable dynamic OG generation, consider deploying to Cloudflare Pages with
 * `@cloudflare/next-on-pages` (supports full Next.js routing on Workers), or
 * provide a static OG image via metadata.
 */

